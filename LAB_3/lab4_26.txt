Лабораторна робота №3
Знаходження алгебраїчних многочленів найкращого квадратичного
наближення методом найменших квадратів.
Мета: Вивчити метод найменших квадратів знаходження алгебраїчних
многочленів найкращого квадратичного наближення.
Основні відомості:
Нехай нам задана сітка "! # "" #!# "! в вузлах якої відомі значення деякої
функції " ! . Розглянемо методику побудови квадратичного наближення
алгебраїчним многочленом за методом найменших квадратів. Шукатимемо
апроксимуючий многочлен у вигляді
!

! ( #) = "$ " # " .
" =!

Степінь многочлена ! вибираємо меншим від загального числа вузлів сітки
! . Коефіцієнти "! знаходимо з умови мінімуму суми квадратів відхилень
апроксимуючого многочлена у вузлах:
!

# = # (! # $" $ " % " )

!

" ="

Якщо відома точність значень функції у вузлах ! ! , то використовуючи
поняття ваги

# ! "! + !#! + $$$ + ! !!
"" =
!"
! +#
можна отримати уточнену формулу:
!

# = $ ! " ( $ " # " ( %" ) )

!

" ="

В цьому випадку апроксимуюча функція буде ближче проходити до вузлів,
в яких значення функції отримано точніше.
Для знаходження коефіцієнтів "! ,, обчислимо частинні похідні і
прирівняємо їх до нуля:
!!
!"
!"
= ! ...
= ! ...
=!
!"!
!#!
!#!
Отримаємо систему лінійних алгебраїчних рівнянь рівнянь:
!

# ! ( $ + $ % + ### + $ % " & ) = !
#

# =!
!

!

" #

"
" #

#

# ! ( $ + $ % + ### + $ % " & ) % = !
# =!

#

!

" #

"
" #

#

#

!

# ! ( $ + $ % + ### + $ % " & ) % = !
#

!

"
" #

" #

#

"
#

# =!

Запишемо систему в компактнішій формі
"!! #! + "!"#" + ### + "! ! #! = $!
"!" #" + "!!#! + ### + "!! #! = $!
...
"! ! #! + "!"#" + ### + "!! #! = $!
Ми ввели скорочені позначення
!

%"# = " !$ &$" +#
$ =!
!

$" = " !# %# &#"
# =!

В загальному виникає проблема оптимального вибору ступеня многочлена
! . Як правило використовують наступну процедуру. Нехай величина !
визначає точність нашої апроксимації. Тоді для деякого значення ! обчислюють
дисперсію
!

"=

$ (! ( # ) # $ )
" ="

"

!

"

! +#
Якщо ! !" , то збільшують ! , якщо ж ! ! " , то, навпаки, зменшують ! .
Якщо ! # " , то значення ! є оптимальним. Якщо вийде, що !!"# ! " , то
необхідно використовувати інші методи для побудови апроксимаційного
многочлена.
Для знаходження коефіцієнтів апроксимуючого алгебраїчного многочлена
виникає задача розв’язку системи лінійних алгебраїчних рівнянь. Чисельні
методи, які використовуються для розв’язку системи лінійних алгебраїчних
рівнянь можна розділити на прямі і ітераційні методи. Серед прямих методів
найбільш широко використовується метод Гауса і його модифікації. Розглянемо
метод Гауса з вибором головного елемента. Суть методу полягає в приведенні
системи рівнянь до системи рівнянь з трикутною матрицею, з якої потім
послідовно знаходять значення всіх невідомих. Таким чином метод Гауса
складається з двох етапів: прямого ходу і зворотного ходу. Для знаходження
розв’язку необхідно виконати !! ! операцій. Розглянемо його особливості на
прикладі системи лінійних алгебраїчних рівнянь з ! невідомими:
#!! $! + #!" $" + ! + #!! $! = %! #

!
#"! $! + #" " $" + ! + #"! $! = %" #
!
#!! $! + #! " $" + ! + #!! $! = %! $
яка може бути представлена у матричній формі !" = # .

Позначимо через ! верхню трикутну матрицю, яку ми отримаємо після
прямого ходу методу Гауса, а через ! - перетворений вектор вільних членів.
Найпростіше програмно реалізувати вибір головного елемента в
стовпчику. Вибір найбільшого по модулю головного елементу дає змогу
мінімізувати похибку округлень.
Приймемо, що всі елементи матриці ! рівні нулю #!" = ! .
Розглянемо перший стовпчик. Нехай "!! – найбільший за модулем його
елемент. Виконаємо перестановку 1 і ! рядків місцями.
"#$% = &! ! ,

#! ! = #"! ,
( ! = !" " )
#!" = $%&'
Далі з допомогою елемента !!! виключають решту елементів першого
стовпчика починаючи з другого і до ! :
"! ! = #! !
( ! = !"!" " )
!! = "!
! ! = !"!" " "
#
#!"!"# = #!" ! #" " !"
# # = !"!" " $
#""
%
&
"
#!!"# = #! ! #" !"
( ! = !"!" " )
"""

де #!" ! $! - елементи перетвореної до трикутного вигляду матриці. $"#( ) елементи вихідної матриці після ! - перетворення.
Розглянемо далі другий стовпчик. Відкидаємо перший елемент !!" і серед
!

усіх "!("!) ( ! = !! " ) вибираємо найбільший за модулем. Перестановкою рядків
розміщаємо його в другому рядку. З його допомогою виключаємо всі інші
елементи другого стовпчика.
"$! = #$!"#!
( ! = !! " )

! $ = "$!"#
( ")

(!)

#!" = #!"

(!)
(!) #! "
! #" " (!)

! ! = !! " "
# # = !! " $
%
&

#""
!"#
!$#
!"#
!"# "! $
#! = #! ! #$ !"# ( ! = !!" )
"$$
Розглянемо k-й крок. В k-му стовпчику серед усіх елементів #"!! ! !"# ( ! = " ! # )
вибираємо найбільший за модулем. Перестановкою рядків переміщаємо його в
! -й рядок.
#!" = $!"! ! !"#
(! = " ! #)

"! = #(!! !!)

(! )

( ! !!)

$ "# = $ "#

( ! !!)
( ! !!) $ "!
! $ !#
( ! !!)

$ !!

( !! " = # + "! $ )

( ! !!)
( ! !!) # "!
! $!
( ! !!)

( ! = " + !" # )
# !!
В результаті ми отримаємо систему рівнянь ! ! " = # , де матриця U має
трикутний вигляд. Описана процедура називається прямим ходом методу Гауса.
На зворотному ході методу Гауса можна знайти значення невідомих ! :
"
# %! = $ !
#
& !!
#
!
#
( $ !!! ! & !!!"! % ! )
$ % !!! =
&
!
!
!"
!
!
!
#
#
!
%
&
# % " = ! ' $ " ! , & "# % # (
#+
& "" )
# =" +!
*
(! )

( ! !!)

$" = $"

Хід роботи.
1. Скласти програму для табуляції заданих даних про температуру на
відрізку [ "! " "! ] з кроком # = "! ! "! , де ! - 24 місяці задане число
!

вузлів. Отриману сукупність вузлів {"! ! #! } використати в подальшому
як набір вхідних даних.
2. Скласти програму, яка для заданої табличної функції {"! ! # ! }
методом найменших квадратів знаходить апроксимуючий
многочлен степені m. В програмі записати наступні функції:
зчитування вхідних даних з текстового файлу – масивів "! ! #! ! ! = "!$ ,
функції формування масивів ! та ! , функцію розв’язування
системи лінійних алгебраїчних рівнянь методом Гауса з вибором
головного елементу по стовпцях, функцію обчислення алгебраїчного
многочлена найкращого квадратичного наближення по відомому
масиву
коефіцієнтів
функцію
обчислення
похибки
!,
! ( !) = " ( !) #" ( !) .
3. Знайти апроксимуючий многочлен і обчислити дисперсію для
випадків

! = !"!"!# .

Побудувати графік залежності величини

дисперсії від степені апроксимуючого многочлена. Вибрати
оптимальне значення степені
дисперсії.

!

виходячи з умови мінімуму

" ! "!
#!!

4. Протабулювати на відрізку [ "! " "! ] з кроком #" = !

похибку для

випадків ! = !"!"!# . Побудувати графіки цих функцій.
5. Результати виконання лабораторної роботи оформити у вигляді
звіту.
Розроблена програма повинна:
1. Зчитувати середньомісячні температури з CSV.
2. Будувати квадратичний або вищий поліном методом найменших
квадратів.
3. Обчислювати дисперсію для різних степенів полінома і вибирає
оптимальний.
4. Створювати графік апроксимації та фактичних даних.
5. Табулювати і будувати графік похибки апроксимації.
6. Екстраполювати прогноз температури на наступні 3 місяці.
Приклад даних у файлі csv
Month,Temp
1,-2
2,0
3,5
4,10
5,15
6,20
7,23
8,22
9,17
10,10
11,5
12,0
13,-10
14,3
15,7
16,13
17,19
18,20
19,22
20,21
21,18
22,15
23,10
24,3

Псевдокод:
# ------------------------------# 1. Вхідні дані
# ------------------------------x = [1,2,...,24]
# місяці
y = [-2,0,5,10,15,20,...,3] # температури
# ------------------------------# 2. Функції МНК
# ------------------------------function form_matrix(x, m):
A = нульова матриця розміром (m+1, m+1)
for i = 0 to m:
for j = 0 to m:
A[i,j] = сума(x[k]^(i+j)) по всіх k
return A
function form_vector(x, y, m):
b = нульовий вектор розміром (m+1)
for i = 0 to m:
b[i] = сума(y[k] * x[k]^i) по всіх k
return b
function gauss_solve(A, b):
# Прямий хід з вибором головного елемента
for k = 0 to n-1:
знайти рядок max_row з найбільшим |A[i,k]|
поміняти рядки k та max_row місцями у A та b
for i = k+1 to n-1:
factor = A[i,k] / A[k,k]
A[i,k:] = A[i,k:] - factor * A[k,k:]
b[i] = b[i] - factor * b[k]
# Зворотній хід
x_sol = нульовий вектор розміром n
for i = n-1 downto 0:
x_sol[i] = (b[i] - сума(A[i,j]*x_sol[j] для j=i+1..n-1)) / A[i,i]
return x_sol
function polynomial(x, coef):
y_poly = нульовий вектор того ж розміру, що x
for i = 0 to length(coef)-1:
y_poly += coef[i] * x^i
return y_poly
function variance(y_true, y_approx):

return середнє((y_true - y_approx)^2)
# ------------------------------# 3. Вибір оптимального ступеня полінома
# ------------------------------max_degree = 4
for m = 1 to max_degree:
A = form_matrix(x, m)
b_vec = form_vector(x, y, m)
coef = gauss_solve(A, b_vec)
y_approx = polynomial(x, coef)
var = variance(y, y_approx)
зберегти var у список variances
optimal_m = індекс мінімального значення у variances + 1
# ------------------------------# 4. Побудова апроксимації
# ------------------------------A = form_matrix(x, optimal_m)
b_vec = form_vector(x, y, optimal_m)
coef = gauss_solve(A, b_vec)
y_approx = polynomial(x, coef)
# ------------------------------# 5. Прогноз на наступні 3 місяці
# ------------------------------x_future = [25,26,27]
y_future = polynomial(x_future, coef)
# ------------------------------# 6. Похибка апроксимації
# ------------------------------error = y - y_approx
# ------------------------------# 7. Вивід результатів
# ------------------------------друкувати дисперсії для різних ступенів
друкувати оптимальний ступінь
друкувати прогноз y_future
побудувати графіки: фактичні дані, апроксимація, похибка

